<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('_article_welcome') }}">Article</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                {% set route = app.request.get('_route') %}
                <li{% if route == '_article_welcome' %} class="active"{% endif %}><a href="{{ path('_article_welcome') }}">Home</a></li>
            </ul>
            {% if user %}
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown{% if route == '_user' %} active{% endif %}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ user.displayName }} <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('_user', {'id': user.id}) }}">Profile</a></li>
                            <li><a href="{{ path('_article_welcome') }}">Logout</a></li>
                        </ul>
                    </li>
                </ul>
            {% endif %}
        </div>
        <!--/.nav-collapse -->
    </div>
</div>